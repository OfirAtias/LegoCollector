# Lego Collector 🧱

A full-stack system for managing a Lego collection, featuring a .NET Core server and a React client.

## 🛠️ Tech Stack

- **Server:** .NET Core Web API, JWT Authentication, Repository Pattern.
- **Client:** React (Vite), Custom CSS Design.

---

## 🚀 How to Run

### 1. Start the Server

Open a terminal in the `Server` folder and run:

```bash
cd Server
dotnet run
The server is now running in the background.

2. Start the Client
Open a new terminal in the Client folder and run:

Bash

cd Client
npm install
npm run dev
Click the link shown in the terminal (usually http://localhost:5173) to open the app.
```

🔑 Login Credentials

To access the system, use the following credentials:

Username: admin

Password: 1234
